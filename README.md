Small project for file compression in Rust.

This is for learning purposes.

To run it:
- ```cargo init```
- ```cargo run```
- ```cargo run guide.pdf comporessed_file``` (to get your compressed file!)
